"""Functions Framework HTTP entry point — generated by build_function_packages.sh.
Wraps job.run_ingest() (or run_extraction, run_analysis — whichever this
function's job module exposes) so Cloud Scheduler's authenticated HTTP call has
something to invoke. Kept separate from job.py so the underlying script stays a
plain, directly-runnable module for local dev (`python -m etl.ingest_feeds`).
"""
import functions_framework
from job import {ENTRYPOINT_FN}


@functions_framework.http
def main(request):
    result = {ENTRYPOINT_FN}()
    return result, 200
